import * as React from 'react'
import { View, Text } from 'react-native'

export interface Props {}

const ReNotificationList = ({}: Props) => {
  return (
    <View>
      <Text>ReNotificationList</Text>
    </View>
  )
}

export default ReNotificationList
