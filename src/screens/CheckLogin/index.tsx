import * as React from 'react'
import { View, Text } from 'react-native'

export interface Props {}

const CheckLoginScreen = ({}: Props) => {
  return (
    <View>
      <Text>CheckLoginScreen</Text>
    </View>
  )
}

export default CheckLoginScreen
