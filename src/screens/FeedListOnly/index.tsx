import * as React from 'react'
import { View, Text } from 'react-native'

export interface Props {}

const FeedListOnlyScreen = ({}: Props) => {
  return (
    <View>
      <Text>FeedListOnlyScreen</Text>
    </View>
  )
}

export default FeedListOnlyScreen
